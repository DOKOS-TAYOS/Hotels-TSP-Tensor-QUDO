# Solver and experiment configuration
n_cities: 5
n_instances: 100
solver: cudaq  # cudaq | cirq | simulated_annealing
formulation: qubo  # qubo | tqudo (native qudits, Cirq/SA only) | tqudo_virtual (qubit emulation, Cirq/CUDA-Q)
qaoa_depth: [1,2,3]