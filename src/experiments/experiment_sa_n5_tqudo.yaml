# Solver and experiment configuration
n_cities: 5
n_instances: 1000
solver: simulated_annealing  # cudaq | cirq | simulated_annealing
formulation: tqudo  # qubo | tqudo (native qudits, Cirq/SA only) | tqudo_virtual (qubit emulation, Cirq/CUDA-Q)